https://davidwalsh.name/php-notifications

INSERT INTO `emaildata`(`domain`, `email`, `adate`) VALUES ('pepipost.com','pepis@pepipost.com',now()) ON DUPLICATE KEY UPDATE adate=now();


CREATE TABLE `emaildata` (
 `domain` varchar(255) DEFAULT NULL,
 `email` varchar(255) DEFAULT NULL,
 `adate` date DEFAULT NULL,
 UNIQUE KEY `email` (`email`)
);

CREATE TABLE `socialdata` (
 `domain` varchar(255) NOT NULL,
 `link` varchar(255) NOT NULL,
 `adate` date NOT NULL,
 UNIQUE KEY `link` (`link`)
);

CREATE TABLE `domainfound` (
 `domain` varchar(255) NOT NULL,
 `foundfrom` varchar(255) NOT NULL,
 `status` int(2) NOT NULL DEFAULT 0,
 `adate` date NOT NULL,
 UNIQUE KEY `domain` (`domain`)
);